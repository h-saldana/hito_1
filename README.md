# API under construction
### It is an API designed to register users. This project is constructed whit Node.js and Express. This API is built around milestones: the `master` branch represents Milestone 1, proceeding sequentially up to Milestone 6, which is the completed API.


### To run the project, use the following:

```bash
npm run dev
```
