# API under construction
### It is an API designed to register users via login.

## Installation
### - It uses PostgreSQL as the database
```bash
docker compose up -d 
```
### - Create a container in Docker.

```bash
docker compose up --build
```
### To run the project, use the following:

```bash
docker compose up --build
```
